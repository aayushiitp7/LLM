# =============================================================
# Frontend Environment — copy to .env.local for local dev
# In Vercel: set these in Project → Settings → Environment Variables
# =============================================================

# Backend API URL — update after deploying the backend
NEXT_PUBLIC_API_URL=https://edip-backend.vercel.app

# NextAuth (optional — currently stubbed, uncomment when enabling auth)
# NEXTAUTH_URL=https://edip-frontend.vercel.app
# NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32
